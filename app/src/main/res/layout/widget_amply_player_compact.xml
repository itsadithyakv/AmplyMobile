<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_widget_amply_compact"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:baselineAligned="false"
    android:paddingHorizontal="12dp"
    android:paddingVertical="7dp"
    android:weightSum="2">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:paddingStart="0dp"
        android:paddingEnd="8dp">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="@font/gilroy_bold"
                android:maxLines="1"
                android:text="@string/app_name"
                android:textColor="#FFFFFFFF"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/widget_artist"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:fontFamily="@font/gilroy_regular"
                android:maxLines="1"
                android:text="@string/widget_no_song"
                android:textColor="#D8FFFFFF"
                android:textSize="11sp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center_vertical|end"
        android:orientation="horizontal"
        android:baselineAligned="false">

        <ImageButton
            android:id="@+id/widget_previous"
            android:layout_width="26dp"
            android:layout_height="30dp"
            android:background="@android:color/transparent"
            android:contentDescription="@string/widget_previous"
            android:padding="8dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_amply_previous"
            android:tint="#B8FFFFFF" />

        <ImageButton
            android:id="@+id/widget_play_pause"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_marginHorizontal="3dp"
            android:background="@drawable/bg_widget_play"
            android:contentDescription="@string/widget_play_pause"
            android:padding="9dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_amply_play"
            android:tint="#FF160B02" />

        <ImageButton
            android:id="@+id/widget_next"
            android:layout_width="26dp"
            android:layout_height="30dp"
            android:background="@android:color/transparent"
            android:contentDescription="@string/widget_next"
            android:padding="8dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_amply_next"
            android:tint="#B8FFFFFF" />
    </LinearLayout>
</LinearLayout>
